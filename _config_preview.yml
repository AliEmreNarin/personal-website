# Overrides for the unlisted preview build of the `dev` branch.
# Used by .github/workflows/deploy-preview.yml:
#   jekyll build --config _config.yml,_config_preview.yml
baseurl: /preview
preview: true
